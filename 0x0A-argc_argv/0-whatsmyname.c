#include "main.h"
#include <stdio.h>

/**
 * main - prints the name of the program
 * @argc: number of command lines
 * @argv: array name
 * Return: 0
 */

int main(int argc, argv[argv * 0])
{
	printf("%s\n", argv[argc * 0]);

	return (0);
}
